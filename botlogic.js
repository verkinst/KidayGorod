class BotLogic {
    
    Bot(difficulty, ) {
        this.difficulty = difficulty;
        if (difficulty==="hard") {
            this.cardWeights=[];
        }
    }

    calculateCardWeight(card){
        
    }

    rerollDecision(){
        return true;
    }


}